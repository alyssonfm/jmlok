<project name="C Sharp Compiler" default="compile_project" basedir="..">
	
	<property name="Output.exe" location="${build_dir}" />	
	<property name="cscExec" value="msbuild" />

	<target name="compile_project">
		<echo>
			Compiling with C Sharp compiler.
		</echo>
	
		<exec command="${cscExec}">
			<arg value="/p:CodeContractsEnableRuntimeChecking=true;CodeContractsAssemblyMode=0;CodeContractsRuntimeThrowOnFailure=true;OutputPath=${Output.exe}"/>
			<arg value="${source_folder}\CodeContractsTest.sln"/>
		</exec>
	</target>

</project>
